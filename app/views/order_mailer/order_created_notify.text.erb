Nouvelle commande numéro <%= @order.id %>

Bonjour,

Voici le récapitulatif de la commande :

Adresse de livraison

<%= @order.user.first_name + " " + @order.user.last_name%>
<%= @order.user.address%>
<%= @order.user.city%>
<%= @order.user.postal_code%>
Suisse
Téléphone : <%= @order.user.phone%>

Informations

Livraison: 
<% if @order.shipping == 'poste' %>
  Poste
  Numéro de suivi: <%= @order.tracking %>
<% else %>
  Retrait au magasin
<% end %>
Paiement:
  <%= @order.payment %>
Etat du paiement:
  <%= @order.is_paid %>
Etat de la commande:
  <%= @order.is_ready %>
Date de la commande:<%= @order.created_at %>
<% if @order.discount %>
  Réduction:<%= @order.discount %> CHF
<% end %>
Montant total de la commande:<%= @total  %> CHF

Produit / Prix unitaire /  Quantité  /  Prix total
<% @buys.each do |buy| %>
  <%= buy.product.name %> /  <%= buy.price %>  /  <%= buy.quantity %> / <%= buy.price * buy.quantity  %>
<% end %>
